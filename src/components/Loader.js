import React from 'react'
import { Colors } from '../utility'
const Loader = () => {
  <div class="spinner-border" style={{ color: Colors.blue }} role="status">
    <span class="sr-only">Loading...</span>
  </div>
}
export default Loader